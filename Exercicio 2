x= int(input("Digite um numero para descobrir o fatorial:  "))
    
def fatorial(x):
    if x<0: return ("nao existe")
    if x==0: return 1
    if (x > 1): return x* fatorial(x-1)
    else:
        return x
        
print ("O valor do fatorial eh: ",fatorial(x))
